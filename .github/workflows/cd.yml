# Workflow de Integração Contínua (CI)
# Objetivo: executar testes unitários a cada push na branch main.
# Estrutura mantida simples e preparada para futuras expansões.

name: Continuous Deployment
on:
  push:
    branches:
      - main

env:
  NODE_VERSION: '24'
  VERCEL_TOKEN: ${{ secrets.VERCELL_TOKEN }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_SCOPE: 610thiago-souza-s-projects1
  

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest

    steps:
      # Realiza o checkout do código-fonte
      - name: Checkout repository
        uses: actions/checkout@v6.0.3

      # Configura a versão do Node.js utilizada no projeto
      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      # Habilita o Corepack para gerenciamento do Yarn
      - name: Enable Corepack
        run: corepack enable

      # Instala as dependências utilizando o lockfile
      - name: Install dependencies
        run: yarn install

      # Executa os testes unitários com Vitest
      - name: Run unit tests
        run: yarn test

  build-and-deploy:
    name: Build & Deploy Vercel Preview
    needs: [unit-tests]
    runs-on: ubuntu-latest
    outputs:
      deployment-url: ${{ steps.deploy.outputs.deployment-url }}

    steps:
      # Realiza o checkout do código-fonte
      - name: Checkout repository
        uses: actions/checkout@v6.0.3

      # Configura a versão do Node.js utilizada no projeto
      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      # Habilita o Corepack para gerenciamento do Yarn
      - name: Enable Corepack
        run: corepack enable

      # Instala as dependências utilizando o lockfile
      - name: Install dependencies
        run: yarn install

      # Obter as configurações do vercel
      - name: Pull Vercel Config
        run: |
          yarn vercel pull --yes \
          --environment=preview \
          --token=$VERCEL_TOKEN  \
          --scope=$VERCEL_SCOPE
      
      # Build para o Vercel
      - name: Build Vercel
        run: yarn vercel build --token=$VERCEL_TOKEN

      # Deploy preview no vercel
      - name: Deploy preview
        id: deploy
        run: |
          URL=$(yarn --silent vercel deploy --prebuilt --target=preview --token=$VERCEL_TOKEN --scope=$VERCEL_SCOPE)
          echo "deployment-url=${URL}" >> $GITHUB_OUTPUT

  e2e-tests:
    name: Run E2E Tests
    needs: [build-and-deploy]
    runs-on: ubuntu-latest

    steps:
      # Realiza o checkout do código-fonte
      - name: Checkout repository
        uses: actions/checkout@v6.0.3

      # Configura a versão do Node.js utilizada no projeto
      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      # Habilita o Corepack para gerenciamento do Yarn
      - name: Enable Corepack
        run: corepack enable

      # Instala as dependências utilizando o lockfile
      - name: Install dependencies
        run: yarn install

      # Instalação do Chromium e dependências do OS
      - name: Install Playwright Browser
        run: npx playwright install --with-deps chromium

      # Obter a configuração de preview para fornecer o DATABASE_URL aos testes
      - name: Pull Preview Vercel Config for E2E
        run: |
          yarn vercel pull --yes \
          --environment=preview \
          --token=$VERCEL_TOKEN \
          --scope=$VERCEL_SCOPE

      # Exporta o DATABASE_URL do preview para o processo do Playwright
      - name: Load Preview Database URL
        run: |
          DATABASE_URL=$(grep '^DATABASE_URL=' .vercel/.env.preview.local | head -n 1 | cut -d= -f2- | sed 's/^=//; s/^"//; s/"$//')
          echo "DATABASE_URL=$DATABASE_URL" >> $GITHUB_ENV

      # Execução dos Testes de Regressão
      - name: Run E2E Regression Tests
        run: yarn playwright test
        env:
          BASE_URL: ${{ needs.build-and-deploy.outputs.deployment-url }}

      # Relatório de testes Automatizados
      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7.0.1
        with:
          name: playwright-report
          path: playwright-report

      #Publicar report JSON do testDino
      - name: Publish Testdino Report
        if: ${{ !cancelled() }}
        run: npx tdpw upload ./playwright-report --token=${{ secrets.TD_TOKEN }}

  promote:
    name: Promote to Production
    needs: [build-and-deploy, e2e-tests]
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.3

      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install

      - name: Pull Vercel Config for Production
        run: |
          yarn vercel pull --yes \
          --environment=production \
          --token=$VERCEL_TOKEN \
          --scope=$VERCEL_SCOPE

      # Produção: GitHub Secrets são a fonte confiável no CI (Vercel Production costuma vir vazio no pull).
      - name: Prepare Vite production environment
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL_PROD }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY_PROD }}
          VITE_SUPABASE_PROJECT_ID: ${{ secrets.VITE_SUPABASE_PROJECT_ID_PROD }}
        run: |
          missing=0
          for var in VITE_SUPABASE_URL VITE_SUPABASE_PUBLISHABLE_KEY VITE_SUPABASE_PROJECT_ID; do
            if [ -z "${!var}" ]; then
              echo "::error::Secret ${var}_PROD ausente. Adicione em GitHub > Settings > Secrets and variables > Actions."
              missing=1
            fi
          done
          [ "$missing" -eq 1 ] && exit 1

          printf 'VITE_SUPABASE_URL=%s\n' "$VITE_SUPABASE_URL" > .env.production.local
          printf 'VITE_SUPABASE_PUBLISHABLE_KEY=%s\n' "$VITE_SUPABASE_PUBLISHABLE_KEY" >> .env.production.local
          printf 'VITE_SUPABASE_PROJECT_ID=%s\n' "$VITE_SUPABASE_PROJECT_ID" >> .env.production.local
          echo "Variáveis de produção carregadas via GitHub Secrets"

      - name: Build Vercel for Production
        run: yarn vercel build --prod --token=$VERCEL_TOKEN --scope=$VERCEL_SCOPE

      - name: Verify Supabase config in production bundle
        run: |
          PROJECT_ID=$(grep '^VITE_SUPABASE_PROJECT_ID=' .env.production.local | cut -d= -f2- | tr -d '"')
          JS_FILE=$(find .vercel/output/static/assets dist/assets -name 'index-*.js' 2>/dev/null | head -1)
          if [ -z "$JS_FILE" ]; then
            echo "::error::Bundle JS de produção não encontrado após o build."
            exit 1
          fi
          if ! grep -q "supabase.co" "$JS_FILE"; then
            echo "::error::Bundle sem supabase.co — VITE_SUPABASE_URL não foi embutida no build."
            exit 1
          fi
          if ! grep -q "$PROJECT_ID" "$JS_FILE"; then
            echo "::error::Bundle sem o project ID de produção ($PROJECT_ID)."
            exit 1
          fi
          echo "Bundle de produção verificado: $JS_FILE"

      - name: Deploy to Production
        run: |
          yarn vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN --scope=$VERCEL_SCOPE
        
    